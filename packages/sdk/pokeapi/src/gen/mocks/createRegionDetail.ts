/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { RegionDetail } from "../models/RegionDetail.ts";
import { createGenerationSummary } from "./createGenerationSummary.ts";
import { createLocationSummary } from "./createLocationSummary.ts";
import { createPokedexSummary } from "./createPokedexSummary.ts";
import { createRegionName } from "./createRegionName.ts";
import { faker } from "@faker-js/faker";

export function createRegionDetail(data?: Partial<RegionDetail>): RegionDetail {
  return {
    ...{
      id: faker.number.int(),
      name: faker.string.alpha({ length: 200 }),
      get locations() {
        return faker.helpers.multiple(() => createLocationSummary());
      },
      get main_generation() {
        return faker.helpers.arrayElement<any>([createGenerationSummary(), null]);
      },
      get names() {
        return faker.helpers.multiple(() => createRegionName());
      },
      get pokedexes() {
        return faker.helpers.multiple(() => createPokedexSummary());
      },
      version_groups: faker.helpers.multiple(() => ({
        name: faker.string.alpha(),
        url: faker.internet.url(),
      })),
      ...(data || {}),
    },
  };
}
