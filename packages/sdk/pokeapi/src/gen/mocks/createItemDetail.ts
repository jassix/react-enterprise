/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ItemDetail } from "../models/ItemDetail.ts";
import { createItemCategorySummary } from "./createItemCategorySummary.ts";
import { createItemEffectText } from "./createItemEffectText.ts";
import { createItemFlavorText } from "./createItemFlavorText.ts";
import { createItemFlingEffectSummary } from "./createItemFlingEffectSummary.ts";
import { createItemGameIndex } from "./createItemGameIndex.ts";
import { createItemName } from "./createItemName.ts";
import { faker } from "@faker-js/faker";

export function createItemDetail(data?: Partial<ItemDetail>): ItemDetail {
  return {
    ...{
      id: faker.number.int(),
      name: faker.string.alpha({ length: 200 }),
      cost: faker.number.int({ min: -2147483648, max: 2147483647 }),
      fling_power: faker.number.int({ min: -2147483648, max: 2147483647 }),
      get fling_effect() {
        return createItemFlingEffectSummary();
      },
      attributes: faker.helpers.multiple(() => ({
        name: faker.string.alpha(),
        url: faker.internet.url(),
      })),
      get category() {
        return createItemCategorySummary();
      },
      get effect_entries() {
        return faker.helpers.multiple(() => createItemEffectText());
      },
      get flavor_text_entries() {
        return faker.helpers.multiple(() => createItemFlavorText());
      },
      get game_indices() {
        return faker.helpers.multiple(() => createItemGameIndex());
      },
      get names() {
        return faker.helpers.multiple(() => createItemName());
      },
      held_by_pokemon: faker.helpers.multiple(() => ({
        pokemon: { name: faker.string.alpha(), url: faker.internet.url() },
        "version-details": faker.helpers.multiple(() => ({
          rarity: faker.number.int(),
          version: { name: faker.string.alpha(), url: faker.internet.url() },
        })),
      })),
      sprites: { default: faker.internet.url() },
      baby_trigger_for: { url: faker.internet.url() },
      machines: faker.helpers.multiple(() => ({
        machine: faker.internet.url(),
        version_group: { name: faker.string.alpha(), url: faker.internet.url() },
      })),
      ...(data || {}),
    },
  };
}
