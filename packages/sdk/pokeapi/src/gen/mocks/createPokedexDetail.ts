/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { PokedexDetail } from "../models/PokedexDetail.ts";
import { createPokedexDescription } from "./createPokedexDescription.ts";
import { createPokedexName } from "./createPokedexName.ts";
import { createRegionSummary } from "./createRegionSummary.ts";
import { faker } from "@faker-js/faker";

export function createPokedexDetail(data?: Partial<PokedexDetail>): PokedexDetail {
  return {
    ...{
      id: faker.number.int(),
      name: faker.string.alpha({ length: 200 }),
      is_main_series: faker.datatype.boolean(),
      get descriptions() {
        return faker.helpers.multiple(() => createPokedexDescription());
      },
      get names() {
        return faker.helpers.multiple(() => createPokedexName());
      },
      pokemon_entries: faker.helpers.multiple(() => ({
        entry_number: faker.number.int(),
        pokemon_species: { name: faker.string.alpha(), url: faker.internet.url() },
      })),
      get region() {
        return createRegionSummary();
      },
      version_groups: faker.helpers.multiple(() => ({
        name: faker.string.alpha(),
        url: faker.internet.url(),
      })),
      ...(data || {}),
    },
  };
}
