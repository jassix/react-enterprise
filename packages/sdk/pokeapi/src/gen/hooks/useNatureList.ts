/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { NatureListQueryResponse, NatureListQueryParams } from "../models/NatureList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { natureList } from "../clients/natureList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const natureListQueryKey = (params?: NatureListQueryParams) =>
  [{ url: "/api/v2/nature/" }, ...(params ? [params] : [])] as const;

export type NatureListQueryKey = ReturnType<typeof natureListQueryKey>;

export function natureListQueryOptions(
  params?: NatureListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = natureListQueryKey(params);
  return queryOptions<
    NatureListQueryResponse,
    ResponseErrorConfig<Error>,
    NatureListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return natureList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Natures influence how a Pokémon's stats grow. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Nature) for greater detail.
 * @summary List natures
 * {@link /api/v2/nature/}
 */
export function useNatureList<
  TData = NatureListQueryResponse,
  TQueryData = NatureListQueryResponse,
  TQueryKey extends QueryKey = NatureListQueryKey,
>(
  params?: NatureListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        NatureListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? natureListQueryKey(params);

  const query = useQuery(
    {
      ...natureListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
