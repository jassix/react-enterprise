/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { StatRetrieveQueryResponse, StatRetrievePathParams } from "../models/StatRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { statRetrieve } from "../clients/statRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const statRetrieveQueryKey = (id: StatRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/stat/:id/", params: { id: id } }] as const;

export type StatRetrieveQueryKey = ReturnType<typeof statRetrieveQueryKey>;

export function statRetrieveQueryOptions(
  id: StatRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = statRetrieveQueryKey(id);
  return queryOptions<
    StatRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    StatRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return statRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Stats determine certain aspects of battles. Each Pokémon has a value for each stat which grows as they gain levels and can be altered momentarily by effects in battles.
 * @summary Get stat
 * {@link /api/v2/stat/:id/}
 */
export function useStatRetrieve<
  TData = StatRetrieveQueryResponse,
  TQueryData = StatRetrieveQueryResponse,
  TQueryKey extends QueryKey = StatRetrieveQueryKey,
>(
  id: StatRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        StatRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? statRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...statRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
