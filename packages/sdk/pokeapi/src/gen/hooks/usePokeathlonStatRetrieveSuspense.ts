/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokeathlonStatRetrieveQueryResponse,
  PokeathlonStatRetrievePathParams,
} from "../models/PokeathlonStatRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { pokeathlonStatRetrieve } from "../clients/pokeathlonStatRetrieve.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const pokeathlonStatRetrieveSuspenseQueryKey = (
  id: PokeathlonStatRetrievePathParams["id"] | undefined,
) => [{ url: "/api/v2/pokeathlon-stat/:id/", params: { id: id } }] as const;

export type PokeathlonStatRetrieveSuspenseQueryKey = ReturnType<
  typeof pokeathlonStatRetrieveSuspenseQueryKey
>;

export function pokeathlonStatRetrieveSuspenseQueryOptions(
  id: PokeathlonStatRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokeathlonStatRetrieveSuspenseQueryKey(id);
  return queryOptions<
    PokeathlonStatRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    PokeathlonStatRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return pokeathlonStatRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Pokeathlon Stats are different attributes of a Pokémon's performance in Pokéathlons. In Pokéathlons, competitions happen on different courses; one for each of the different Pokéathlon stats. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pok%C3%A9athlon) for greater detail.
 * @summary Get pokeathlon stat
 * {@link /api/v2/pokeathlon-stat/:id/}
 */
export function usePokeathlonStatRetrieveSuspense<
  TData = PokeathlonStatRetrieveQueryResponse,
  TQueryKey extends QueryKey = PokeathlonStatRetrieveSuspenseQueryKey,
>(
  id: PokeathlonStatRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        PokeathlonStatRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokeathlonStatRetrieveSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...pokeathlonStatRetrieveSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
