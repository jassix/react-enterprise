/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  CharacteristicListQueryResponse,
  CharacteristicListQueryParams,
} from "../models/CharacteristicList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { characteristicList } from "../clients/characteristicList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const characteristicListSuspenseQueryKey = (params?: CharacteristicListQueryParams) =>
  [{ url: "/api/v2/characteristic/" }, ...(params ? [params] : [])] as const;

export type CharacteristicListSuspenseQueryKey = ReturnType<
  typeof characteristicListSuspenseQueryKey
>;

export function characteristicListSuspenseQueryOptions(
  params?: CharacteristicListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = characteristicListSuspenseQueryKey(params);
  return queryOptions<
    CharacteristicListQueryResponse,
    ResponseErrorConfig<Error>,
    CharacteristicListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return characteristicList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Characteristics indicate which stat contains a Pokémon's highest IV. A Pokémon's Characteristic is determined by the remainder of its highest IV divided by 5 (gene_modulo). Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Characteristic) for greater detail.
 * @summary List charecterictics
 * {@link /api/v2/characteristic/}
 */
export function useCharacteristicListSuspense<
  TData = CharacteristicListQueryResponse,
  TQueryKey extends QueryKey = CharacteristicListSuspenseQueryKey,
>(
  params?: CharacteristicListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        CharacteristicListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? characteristicListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...characteristicListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
