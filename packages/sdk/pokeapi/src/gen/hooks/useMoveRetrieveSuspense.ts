/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MoveRetrieveQueryResponse, MoveRetrievePathParams } from "../models/MoveRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { moveRetrieve } from "../clients/moveRetrieve.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const moveRetrieveSuspenseQueryKey = (id: MoveRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/move/:id/", params: { id: id } }] as const;

export type MoveRetrieveSuspenseQueryKey = ReturnType<typeof moveRetrieveSuspenseQueryKey>;

export function moveRetrieveSuspenseQueryOptions(
  id: MoveRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = moveRetrieveSuspenseQueryKey(id);
  return queryOptions<
    MoveRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    MoveRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return moveRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Moves are the skills of Pokémon in battle. In battle, a Pokémon uses one move each turn. Some moves (including those learned by Hidden Machine) can be used outside of battle as well, usually for the purpose of removing obstacles or exploring new areas.
 * @summary Get move
 * {@link /api/v2/move/:id/}
 */
export function useMoveRetrieveSuspense<
  TData = MoveRetrieveQueryResponse,
  TQueryKey extends QueryKey = MoveRetrieveSuspenseQueryKey,
>(
  id: MoveRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        MoveRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? moveRetrieveSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...moveRetrieveSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
