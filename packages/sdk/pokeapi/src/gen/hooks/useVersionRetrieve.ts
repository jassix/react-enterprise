/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  VersionRetrieveQueryResponse,
  VersionRetrievePathParams,
} from "../models/VersionRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { versionRetrieve } from "../clients/versionRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const versionRetrieveQueryKey = (id: VersionRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/version/:id/", params: { id: id } }] as const;

export type VersionRetrieveQueryKey = ReturnType<typeof versionRetrieveQueryKey>;

export function versionRetrieveQueryOptions(
  id: VersionRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = versionRetrieveQueryKey(id);
  return queryOptions<
    VersionRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    VersionRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return versionRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Versions of the games, e.g., Red, Blue or Yellow.
 * @summary Get version
 * {@link /api/v2/version/:id/}
 */
export function useVersionRetrieve<
  TData = VersionRetrieveQueryResponse,
  TQueryData = VersionRetrieveQueryResponse,
  TQueryKey extends QueryKey = VersionRetrieveQueryKey,
>(
  id: VersionRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        VersionRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? versionRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...versionRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
