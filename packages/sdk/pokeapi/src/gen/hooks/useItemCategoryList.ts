/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ItemCategoryListQueryResponse,
  ItemCategoryListQueryParams,
} from "../models/ItemCategoryList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { itemCategoryList } from "../clients/itemCategoryList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const itemCategoryListQueryKey = (params?: ItemCategoryListQueryParams) =>
  [{ url: "/api/v2/item-category/" }, ...(params ? [params] : [])] as const;

export type ItemCategoryListQueryKey = ReturnType<typeof itemCategoryListQueryKey>;

export function itemCategoryListQueryOptions(
  params?: ItemCategoryListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = itemCategoryListQueryKey(params);
  return queryOptions<
    ItemCategoryListQueryResponse,
    ResponseErrorConfig<Error>,
    ItemCategoryListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return itemCategoryList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Item categories determine where items will be placed in the players bag.
 * @summary List item categories
 * {@link /api/v2/item-category/}
 */
export function useItemCategoryList<
  TData = ItemCategoryListQueryResponse,
  TQueryData = ItemCategoryListQueryResponse,
  TQueryKey extends QueryKey = ItemCategoryListQueryKey,
>(
  params?: ItemCategoryListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ItemCategoryListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? itemCategoryListQueryKey(params);

  const query = useQuery(
    {
      ...itemCategoryListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
