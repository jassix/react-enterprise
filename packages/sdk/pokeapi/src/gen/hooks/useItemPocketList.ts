/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  ItemPocketListQueryResponse,
  ItemPocketListQueryParams,
} from "../models/ItemPocketList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { itemPocketList } from "../clients/itemPocketList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const itemPocketListQueryKey = (params?: ItemPocketListQueryParams) =>
  [{ url: "/api/v2/item-pocket/" }, ...(params ? [params] : [])] as const;

export type ItemPocketListQueryKey = ReturnType<typeof itemPocketListQueryKey>;

export function itemPocketListQueryOptions(
  params?: ItemPocketListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = itemPocketListQueryKey(params);
  return queryOptions<
    ItemPocketListQueryResponse,
    ResponseErrorConfig<Error>,
    ItemPocketListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return itemPocketList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Pockets within the players bag used for storing items by category.
 * @summary List item pockets
 * {@link /api/v2/item-pocket/}
 */
export function useItemPocketList<
  TData = ItemPocketListQueryResponse,
  TQueryData = ItemPocketListQueryResponse,
  TQueryKey extends QueryKey = ItemPocketListQueryKey,
>(
  params?: ItemPocketListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ItemPocketListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? itemPocketListQueryKey(params);

  const query = useQuery(
    {
      ...itemPocketListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
