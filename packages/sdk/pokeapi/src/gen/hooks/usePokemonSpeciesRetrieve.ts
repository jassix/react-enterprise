/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonSpeciesRetrieveQueryResponse,
  PokemonSpeciesRetrievePathParams,
} from "../models/PokemonSpeciesRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { pokemonSpeciesRetrieve } from "../clients/pokemonSpeciesRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const pokemonSpeciesRetrieveQueryKey = (
  id: PokemonSpeciesRetrievePathParams["id"] | undefined,
) => [{ url: "/api/v2/pokemon-species/:id/", params: { id: id } }] as const;

export type PokemonSpeciesRetrieveQueryKey = ReturnType<typeof pokemonSpeciesRetrieveQueryKey>;

export function pokemonSpeciesRetrieveQueryOptions(
  id: PokemonSpeciesRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonSpeciesRetrieveQueryKey(id);
  return queryOptions<
    PokemonSpeciesRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonSpeciesRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonSpeciesRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description A Pokémon Species forms the basis for at least one Pokémon. Attributes of a Pokémon species are shared across all varieties of Pokémon within the species. A good example is Wormadam; Wormadam is the species which can be found in three different varieties, Wormadam-Trash, Wormadam-Sandy and Wormadam-Plant.
 * @summary Get pokemon species
 * {@link /api/v2/pokemon-species/:id/}
 */
export function usePokemonSpeciesRetrieve<
  TData = PokemonSpeciesRetrieveQueryResponse,
  TQueryData = PokemonSpeciesRetrieveQueryResponse,
  TQueryKey extends QueryKey = PokemonSpeciesRetrieveQueryKey,
>(
  id: PokemonSpeciesRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PokemonSpeciesRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonSpeciesRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...pokemonSpeciesRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
