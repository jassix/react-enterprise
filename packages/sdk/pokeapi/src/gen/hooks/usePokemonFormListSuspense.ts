/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonFormListQueryResponse,
  PokemonFormListQueryParams,
} from "../models/PokemonFormList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { pokemonFormList } from "../clients/pokemonFormList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const pokemonFormListSuspenseQueryKey = (params?: PokemonFormListQueryParams) =>
  [{ url: "/api/v2/pokemon-form/" }, ...(params ? [params] : [])] as const;

export type PokemonFormListSuspenseQueryKey = ReturnType<typeof pokemonFormListSuspenseQueryKey>;

export function pokemonFormListSuspenseQueryOptions(
  params?: PokemonFormListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonFormListSuspenseQueryKey(params);
  return queryOptions<
    PokemonFormListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonFormListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonFormList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Some Pokémon may appear in one of multiple, visually different forms. These differences are purely cosmetic. For variations within a Pokémon species, which do differ in more than just visuals, the 'Pokémon' entity is used to represent such a variety.
 * @summary List pokemon forms
 * {@link /api/v2/pokemon-form/}
 */
export function usePokemonFormListSuspense<
  TData = PokemonFormListQueryResponse,
  TQueryKey extends QueryKey = PokemonFormListSuspenseQueryKey,
>(
  params?: PokemonFormListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        PokemonFormListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonFormListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...pokemonFormListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
