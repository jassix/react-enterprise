/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { BerryListQueryResponse, BerryListQueryParams } from "../models/BerryList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { berryList } from "../clients/berryList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const berryListQueryKey = (params?: BerryListQueryParams) =>
  [{ url: "/api/v2/berry/" }, ...(params ? [params] : [])] as const;

export type BerryListQueryKey = ReturnType<typeof berryListQueryKey>;

export function berryListQueryOptions(
  params?: BerryListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = berryListQueryKey(params);
  return queryOptions<
    BerryListQueryResponse,
    ResponseErrorConfig<Error>,
    BerryListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return berryList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Berries are small fruits that can provide HP and status condition restoration, stat enhancement, and even damage negation when eaten by Pokémon. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Berry) for greater detail.
 * @summary List berries
 * {@link /api/v2/berry/}
 */
export function useBerryList<
  TData = BerryListQueryResponse,
  TQueryData = BerryListQueryResponse,
  TQueryKey extends QueryKey = BerryListQueryKey,
>(
  params?: BerryListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        BerryListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? berryListQueryKey(params);

  const query = useQuery(
    {
      ...berryListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
