/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { VersionListQueryResponse, VersionListQueryParams } from "../models/VersionList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { versionList } from "../clients/versionList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const versionListQueryKey = (params?: VersionListQueryParams) =>
  [{ url: "/api/v2/version/" }, ...(params ? [params] : [])] as const;

export type VersionListQueryKey = ReturnType<typeof versionListQueryKey>;

export function versionListQueryOptions(
  params?: VersionListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = versionListQueryKey(params);
  return queryOptions<
    VersionListQueryResponse,
    ResponseErrorConfig<Error>,
    VersionListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return versionList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Versions of the games, e.g., Red, Blue or Yellow.
 * @summary List versions
 * {@link /api/v2/version/}
 */
export function useVersionList<
  TData = VersionListQueryResponse,
  TQueryData = VersionListQueryResponse,
  TQueryKey extends QueryKey = VersionListQueryKey,
>(
  params?: VersionListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        VersionListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? versionListQueryKey(params);

  const query = useQuery(
    {
      ...versionListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
