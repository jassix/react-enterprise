/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MoveListQueryResponse, MoveListQueryParams } from "../models/MoveList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { moveList } from "../clients/moveList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const moveListQueryKey = (params?: MoveListQueryParams) =>
  [{ url: "/api/v2/move/" }, ...(params ? [params] : [])] as const;

export type MoveListQueryKey = ReturnType<typeof moveListQueryKey>;

export function moveListQueryOptions(
  params?: MoveListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = moveListQueryKey(params);
  return queryOptions<
    MoveListQueryResponse,
    ResponseErrorConfig<Error>,
    MoveListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return moveList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Moves are the skills of Pokémon in battle. In battle, a Pokémon uses one move each turn. Some moves (including those learned by Hidden Machine) can be used outside of battle as well, usually for the purpose of removing obstacles or exploring new areas.
 * @summary List moves
 * {@link /api/v2/move/}
 */
export function useMoveList<
  TData = MoveListQueryResponse,
  TQueryData = MoveListQueryResponse,
  TQueryKey extends QueryKey = MoveListQueryKey,
>(
  params?: MoveListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MoveListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? moveListQueryKey(params);

  const query = useQuery(
    {
      ...moveListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
