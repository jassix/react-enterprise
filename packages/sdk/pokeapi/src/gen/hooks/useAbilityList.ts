/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { AbilityListQueryResponse, AbilityListQueryParams } from "../models/AbilityList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { abilityList } from "../clients/abilityList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const abilityListQueryKey = (params?: AbilityListQueryParams) =>
  [{ url: "/api/v2/ability/" }, ...(params ? [params] : [])] as const;

export type AbilityListQueryKey = ReturnType<typeof abilityListQueryKey>;

export function abilityListQueryOptions(
  params?: AbilityListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = abilityListQueryKey(params);
  return queryOptions<
    AbilityListQueryResponse,
    ResponseErrorConfig<Error>,
    AbilityListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return abilityList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Abilities provide passive effects for Pokémon in battle or in the overworld. Pokémon have multiple possible abilities but can have only one ability at a time. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Ability) for greater detail.
 * {@link /api/v2/ability/}
 */
export function useAbilityList<
  TData = AbilityListQueryResponse,
  TQueryData = AbilityListQueryResponse,
  TQueryKey extends QueryKey = AbilityListQueryKey,
>(
  params?: AbilityListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        AbilityListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? abilityListQueryKey(params);

  const query = useQuery(
    {
      ...abilityListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
