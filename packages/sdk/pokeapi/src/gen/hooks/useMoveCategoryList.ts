/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  MoveCategoryListQueryResponse,
  MoveCategoryListQueryParams,
} from "../models/MoveCategoryList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { moveCategoryList } from "../clients/moveCategoryList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const moveCategoryListQueryKey = (params?: MoveCategoryListQueryParams) =>
  [{ url: "/api/v2/move-category/" }, ...(params ? [params] : [])] as const;

export type MoveCategoryListQueryKey = ReturnType<typeof moveCategoryListQueryKey>;

export function moveCategoryListQueryOptions(
  params?: MoveCategoryListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = moveCategoryListQueryKey(params);
  return queryOptions<
    MoveCategoryListQueryResponse,
    ResponseErrorConfig<Error>,
    MoveCategoryListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return moveCategoryList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Very general categories that loosely group move effects.
 * @summary List move meta categories
 * {@link /api/v2/move-category/}
 */
export function useMoveCategoryList<
  TData = MoveCategoryListQueryResponse,
  TQueryData = MoveCategoryListQueryResponse,
  TQueryKey extends QueryKey = MoveCategoryListQueryKey,
>(
  params?: MoveCategoryListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MoveCategoryListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? moveCategoryListQueryKey(params);

  const query = useQuery(
    {
      ...moveCategoryListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
