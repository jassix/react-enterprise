/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { LocationListQueryResponse, LocationListQueryParams } from "../models/LocationList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { locationList } from "../clients/locationList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const locationListQueryKey = (params?: LocationListQueryParams) =>
  [{ url: "/api/v2/location/" }, ...(params ? [params] : [])] as const;

export type LocationListQueryKey = ReturnType<typeof locationListQueryKey>;

export function locationListQueryOptions(
  params?: LocationListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = locationListQueryKey(params);
  return queryOptions<
    LocationListQueryResponse,
    ResponseErrorConfig<Error>,
    LocationListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return locationList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
 * @summary List locations
 * {@link /api/v2/location/}
 */
export function useLocationList<
  TData = LocationListQueryResponse,
  TQueryData = LocationListQueryResponse,
  TQueryKey extends QueryKey = LocationListQueryKey,
>(
  params?: LocationListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        LocationListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? locationListQueryKey(params);

  const query = useQuery(
    {
      ...locationListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
