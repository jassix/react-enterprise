/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  VersionGroupRetrieveQueryResponse,
  VersionGroupRetrievePathParams,
} from "../models/VersionGroupRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { versionGroupRetrieve } from "../clients/versionGroupRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const versionGroupRetrieveQueryKey = (
  id: VersionGroupRetrievePathParams["id"] | undefined,
) => [{ url: "/api/v2/version-group/:id/", params: { id: id } }] as const;

export type VersionGroupRetrieveQueryKey = ReturnType<typeof versionGroupRetrieveQueryKey>;

export function versionGroupRetrieveQueryOptions(
  id: VersionGroupRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = versionGroupRetrieveQueryKey(id);
  return queryOptions<
    VersionGroupRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    VersionGroupRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return versionGroupRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Version groups categorize highly similar versions of the games.
 * @summary Get version group
 * {@link /api/v2/version-group/:id/}
 */
export function useVersionGroupRetrieve<
  TData = VersionGroupRetrieveQueryResponse,
  TQueryData = VersionGroupRetrieveQueryResponse,
  TQueryKey extends QueryKey = VersionGroupRetrieveQueryKey,
>(
  id: VersionGroupRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        VersionGroupRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? versionGroupRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...versionGroupRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
