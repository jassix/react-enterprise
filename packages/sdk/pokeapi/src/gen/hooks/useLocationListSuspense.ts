/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { LocationListQueryResponse, LocationListQueryParams } from "../models/LocationList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { locationList } from "../clients/locationList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const locationListSuspenseQueryKey = (params?: LocationListQueryParams) =>
  [{ url: "/api/v2/location/" }, ...(params ? [params] : [])] as const;

export type LocationListSuspenseQueryKey = ReturnType<typeof locationListSuspenseQueryKey>;

export function locationListSuspenseQueryOptions(
  params?: LocationListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = locationListSuspenseQueryKey(params);
  return queryOptions<
    LocationListQueryResponse,
    ResponseErrorConfig<Error>,
    LocationListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return locationList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
 * @summary List locations
 * {@link /api/v2/location/}
 */
export function useLocationListSuspense<
  TData = LocationListQueryResponse,
  TQueryKey extends QueryKey = LocationListSuspenseQueryKey,
>(
  params?: LocationListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        LocationListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? locationListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...locationListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
