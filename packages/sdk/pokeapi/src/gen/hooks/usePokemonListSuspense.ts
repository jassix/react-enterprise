/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { PokemonListQueryResponse, PokemonListQueryParams } from "../models/PokemonList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { pokemonList } from "../clients/pokemonList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const pokemonListSuspenseQueryKey = (params?: PokemonListQueryParams) =>
  [{ url: "/api/v2/pokemon/" }, ...(params ? [params] : [])] as const;

export type PokemonListSuspenseQueryKey = ReturnType<typeof pokemonListSuspenseQueryKey>;

export function pokemonListSuspenseQueryOptions(
  params?: PokemonListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonListSuspenseQueryKey(params);
  return queryOptions<
    PokemonListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Pokémon are the creatures that inhabit the world of the Pokémon games. They can be caught using Pokéballs and trained by battling with other Pokémon. Each Pokémon belongs to a specific species but may take on a variant which makes it differ from other Pokémon of the same species, such as base stats, available abilities and typings. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pok%C3%A9mon_(species)) for greater detail.
 * @summary List pokemon
 * {@link /api/v2/pokemon/}
 */
export function usePokemonListSuspense<
  TData = PokemonListQueryResponse,
  TQueryKey extends QueryKey = PokemonListSuspenseQueryKey,
>(
  params?: PokemonListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        PokemonListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...pokemonListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
