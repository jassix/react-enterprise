/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MetaListQueryResponse } from "../models/MetaList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { metaList } from "../clients/metaList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const metaListQueryKey = () => [{ url: "/api/v2/meta/" }] as const;

export type MetaListQueryKey = ReturnType<typeof metaListQueryKey>;

export function metaListQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = metaListQueryKey();
  return queryOptions<
    MetaListQueryResponse,
    ResponseErrorConfig<Error>,
    MetaListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return metaList({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Returns metadata about the current deployed version of the API, including the git commit hash, deploy date, and tag (if any).
 * {@link /api/v2/meta/}
 */
export function useMetaList<
  TData = MetaListQueryResponse,
  TQueryData = MetaListQueryResponse,
  TQueryKey extends QueryKey = MetaListQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        MetaListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? metaListQueryKey();

  const query = useQuery(
    {
      ...metaListQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
