/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  LocationRetrieveQueryResponse,
  LocationRetrievePathParams,
} from "../models/LocationRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { locationRetrieve } from "../clients/locationRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const locationRetrieveQueryKey = (id: LocationRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/location/:id/", params: { id: id } }] as const;

export type LocationRetrieveQueryKey = ReturnType<typeof locationRetrieveQueryKey>;

export function locationRetrieveQueryOptions(
  id: LocationRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = locationRetrieveQueryKey(id);
  return queryOptions<
    LocationRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    LocationRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return locationRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
 * @summary Get location
 * {@link /api/v2/location/:id/}
 */
export function useLocationRetrieve<
  TData = LocationRetrieveQueryResponse,
  TQueryData = LocationRetrieveQueryResponse,
  TQueryKey extends QueryKey = LocationRetrieveQueryKey,
>(
  id: LocationRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        LocationRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? locationRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...locationRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
