/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  AbilityRetrieveQueryResponse,
  AbilityRetrievePathParams,
} from "../models/AbilityRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { abilityRetrieve } from "../clients/abilityRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const abilityRetrieveQueryKey = (id: AbilityRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/ability/:id/", params: { id: id } }] as const;

export type AbilityRetrieveQueryKey = ReturnType<typeof abilityRetrieveQueryKey>;

export function abilityRetrieveQueryOptions(
  id: AbilityRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = abilityRetrieveQueryKey(id);
  return queryOptions<
    AbilityRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    AbilityRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return abilityRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Abilities provide passive effects for Pokémon in battle or in the overworld. Pokémon have multiple possible abilities but can have only one ability at a time. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Ability) for greater detail.
 * {@link /api/v2/ability/:id/}
 */
export function useAbilityRetrieve<
  TData = AbilityRetrieveQueryResponse,
  TQueryData = AbilityRetrieveQueryResponse,
  TQueryKey extends QueryKey = AbilityRetrieveQueryKey,
>(
  id: AbilityRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        AbilityRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? abilityRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...abilityRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
