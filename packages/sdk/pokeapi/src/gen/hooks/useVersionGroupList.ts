/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  VersionGroupListQueryResponse,
  VersionGroupListQueryParams,
} from "../models/VersionGroupList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { versionGroupList } from "../clients/versionGroupList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const versionGroupListQueryKey = (params?: VersionGroupListQueryParams) =>
  [{ url: "/api/v2/version-group/" }, ...(params ? [params] : [])] as const;

export type VersionGroupListQueryKey = ReturnType<typeof versionGroupListQueryKey>;

export function versionGroupListQueryOptions(
  params?: VersionGroupListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = versionGroupListQueryKey(params);
  return queryOptions<
    VersionGroupListQueryResponse,
    ResponseErrorConfig<Error>,
    VersionGroupListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return versionGroupList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Version groups categorize highly similar versions of the games.
 * @summary List version groups
 * {@link /api/v2/version-group/}
 */
export function useVersionGroupList<
  TData = VersionGroupListQueryResponse,
  TQueryData = VersionGroupListQueryResponse,
  TQueryKey extends QueryKey = VersionGroupListQueryKey,
>(
  params?: VersionGroupListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        VersionGroupListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? versionGroupListQueryKey(params);

  const query = useQuery(
    {
      ...versionGroupListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
