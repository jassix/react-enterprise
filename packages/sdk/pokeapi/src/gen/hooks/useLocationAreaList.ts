/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  LocationAreaListQueryResponse,
  LocationAreaListQueryParams,
} from "../models/LocationAreaList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { locationAreaList } from "../clients/locationAreaList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const locationAreaListQueryKey = (params?: LocationAreaListQueryParams) =>
  [{ url: "/api/v2/location-area/" }, ...(params ? [params] : [])] as const;

export type LocationAreaListQueryKey = ReturnType<typeof locationAreaListQueryKey>;

export function locationAreaListQueryOptions(
  params?: LocationAreaListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = locationAreaListQueryKey(params);
  return queryOptions<
    LocationAreaListQueryResponse,
    ResponseErrorConfig<Error>,
    LocationAreaListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return locationAreaList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Location areas are sections of areas, such as floors in a building or cave. Each area has its own set of possible Pokémon encounters.
 * @summary List location areas
 * {@link /api/v2/location-area/}
 */
export function useLocationAreaList<
  TData = LocationAreaListQueryResponse,
  TQueryData = LocationAreaListQueryResponse,
  TQueryKey extends QueryKey = LocationAreaListQueryKey,
>(
  params?: LocationAreaListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        LocationAreaListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? locationAreaListQueryKey(params);

  const query = useQuery(
    {
      ...locationAreaListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
