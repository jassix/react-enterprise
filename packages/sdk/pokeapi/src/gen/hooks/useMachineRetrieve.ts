/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  MachineRetrieveQueryResponse,
  MachineRetrievePathParams,
} from "../models/MachineRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { machineRetrieve } from "../clients/machineRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const machineRetrieveQueryKey = (id: MachineRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/machine/:id/", params: { id: id } }] as const;

export type MachineRetrieveQueryKey = ReturnType<typeof machineRetrieveQueryKey>;

export function machineRetrieveQueryOptions(
  id: MachineRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = machineRetrieveQueryKey(id);
  return queryOptions<
    MachineRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    MachineRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return machineRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Machines are the representation of items that teach moves to Pokémon. They vary from version to version, so it is not certain that one specific TM or HM corresponds to a single Machine.
 * @summary Get machine
 * {@link /api/v2/machine/:id/}
 */
export function useMachineRetrieve<
  TData = MachineRetrieveQueryResponse,
  TQueryData = MachineRetrieveQueryResponse,
  TQueryKey extends QueryKey = MachineRetrieveQueryKey,
>(
  id: MachineRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MachineRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? machineRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...machineRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
