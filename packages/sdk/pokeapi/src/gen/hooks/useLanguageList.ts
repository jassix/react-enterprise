/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { LanguageListQueryResponse, LanguageListQueryParams } from "../models/LanguageList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { languageList } from "../clients/languageList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const languageListQueryKey = (params?: LanguageListQueryParams) =>
  [{ url: "/api/v2/language/" }, ...(params ? [params] : [])] as const;

export type LanguageListQueryKey = ReturnType<typeof languageListQueryKey>;

export function languageListQueryOptions(
  params?: LanguageListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = languageListQueryKey(params);
  return queryOptions<
    LanguageListQueryResponse,
    ResponseErrorConfig<Error>,
    LanguageListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return languageList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Languages for translations of API resource information.
 * @summary List languages
 * {@link /api/v2/language/}
 */
export function useLanguageList<
  TData = LanguageListQueryResponse,
  TQueryData = LanguageListQueryResponse,
  TQueryKey extends QueryKey = LanguageListQueryKey,
>(
  params?: LanguageListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        LanguageListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? languageListQueryKey(params);

  const query = useQuery(
    {
      ...languageListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
