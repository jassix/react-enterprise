/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PalParkAreaListQueryResponse,
  PalParkAreaListQueryParams,
} from "../models/PalParkAreaList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { palParkAreaList } from "../clients/palParkAreaList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const palParkAreaListQueryKey = (params?: PalParkAreaListQueryParams) =>
  [{ url: "/api/v2/pal-park-area/" }, ...(params ? [params] : [])] as const;

export type PalParkAreaListQueryKey = ReturnType<typeof palParkAreaListQueryKey>;

export function palParkAreaListQueryOptions(
  params?: PalParkAreaListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = palParkAreaListQueryKey(params);
  return queryOptions<
    PalParkAreaListQueryResponse,
    ResponseErrorConfig<Error>,
    PalParkAreaListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return palParkAreaList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Areas used for grouping Pokémon encounters in Pal Park. They're like habitats that are specific to Pal Park.
 * @summary List pal park areas
 * {@link /api/v2/pal-park-area/}
 */
export function usePalParkAreaList<
  TData = PalParkAreaListQueryResponse,
  TQueryData = PalParkAreaListQueryResponse,
  TQueryKey extends QueryKey = PalParkAreaListQueryKey,
>(
  params?: PalParkAreaListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PalParkAreaListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? palParkAreaListQueryKey(params);

  const query = useQuery(
    {
      ...palParkAreaListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
