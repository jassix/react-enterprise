/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  BerryRetrieveQueryResponse,
  BerryRetrievePathParams,
} from "../models/BerryRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { berryRetrieve } from "../clients/berryRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const berryRetrieveQueryKey = (id: BerryRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/berry/:id/", params: { id: id } }] as const;

export type BerryRetrieveQueryKey = ReturnType<typeof berryRetrieveQueryKey>;

export function berryRetrieveQueryOptions(
  id: BerryRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = berryRetrieveQueryKey(id);
  return queryOptions<
    BerryRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    BerryRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return berryRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Berries are small fruits that can provide HP and status condition restoration, stat enhancement, and even damage negation when eaten by Pokémon. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Berry) for greater detail.
 * @summary Get a berry
 * {@link /api/v2/berry/:id/}
 */
export function useBerryRetrieve<
  TData = BerryRetrieveQueryResponse,
  TQueryData = BerryRetrieveQueryResponse,
  TQueryKey extends QueryKey = BerryRetrieveQueryKey,
>(
  id: BerryRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        BerryRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? berryRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...berryRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
