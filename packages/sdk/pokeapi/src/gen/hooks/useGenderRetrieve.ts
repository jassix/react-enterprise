/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GenderRetrieveQueryResponse,
  GenderRetrievePathParams,
} from "../models/GenderRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { genderRetrieve } from "../clients/genderRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const genderRetrieveQueryKey = (id: GenderRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/gender/:id/", params: { id: id } }] as const;

export type GenderRetrieveQueryKey = ReturnType<typeof genderRetrieveQueryKey>;

export function genderRetrieveQueryOptions(
  id: GenderRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = genderRetrieveQueryKey(id);
  return queryOptions<
    GenderRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    GenderRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return genderRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Genders were introduced in Generation II for the purposes of breeding Pokémon but can also result in visual differences or even different evolutionary lines. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Gender) for greater detail.
 * @summary Get gender
 * {@link /api/v2/gender/:id/}
 */
export function useGenderRetrieve<
  TData = GenderRetrieveQueryResponse,
  TQueryData = GenderRetrieveQueryResponse,
  TQueryKey extends QueryKey = GenderRetrieveQueryKey,
>(
  id: GenderRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GenderRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? genderRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...genderRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
