/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { StatListQueryResponse, StatListQueryParams } from "../models/StatList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { statList } from "../clients/statList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const statListQueryKey = (params?: StatListQueryParams) =>
  [{ url: "/api/v2/stat/" }, ...(params ? [params] : [])] as const;

export type StatListQueryKey = ReturnType<typeof statListQueryKey>;

export function statListQueryOptions(
  params?: StatListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = statListQueryKey(params);
  return queryOptions<
    StatListQueryResponse,
    ResponseErrorConfig<Error>,
    StatListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return statList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Stats determine certain aspects of battles. Each Pokémon has a value for each stat which grows as they gain levels and can be altered momentarily by effects in battles.
 * @summary List stats
 * {@link /api/v2/stat/}
 */
export function useStatList<
  TData = StatListQueryResponse,
  TQueryData = StatListQueryResponse,
  TQueryKey extends QueryKey = StatListQueryKey,
>(
  params?: StatListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        StatListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? statListQueryKey(params);

  const query = useQuery(
    {
      ...statListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
