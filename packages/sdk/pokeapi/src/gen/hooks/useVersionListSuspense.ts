/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { VersionListQueryResponse, VersionListQueryParams } from "../models/VersionList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { versionList } from "../clients/versionList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const versionListSuspenseQueryKey = (params?: VersionListQueryParams) =>
  [{ url: "/api/v2/version/" }, ...(params ? [params] : [])] as const;

export type VersionListSuspenseQueryKey = ReturnType<typeof versionListSuspenseQueryKey>;

export function versionListSuspenseQueryOptions(
  params?: VersionListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = versionListSuspenseQueryKey(params);
  return queryOptions<
    VersionListQueryResponse,
    ResponseErrorConfig<Error>,
    VersionListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return versionList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Versions of the games, e.g., Red, Blue or Yellow.
 * @summary List versions
 * {@link /api/v2/version/}
 */
export function useVersionListSuspense<
  TData = VersionListQueryResponse,
  TQueryKey extends QueryKey = VersionListSuspenseQueryKey,
>(
  params?: VersionListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        VersionListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? versionListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...versionListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
