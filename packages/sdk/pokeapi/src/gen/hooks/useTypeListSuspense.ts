/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { TypeListQueryResponse, TypeListQueryParams } from "../models/TypeList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { typeList } from "../clients/typeList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const typeListSuspenseQueryKey = (params?: TypeListQueryParams) =>
  [{ url: "/api/v2/type/" }, ...(params ? [params] : [])] as const;

export type TypeListSuspenseQueryKey = ReturnType<typeof typeListSuspenseQueryKey>;

export function typeListSuspenseQueryOptions(
  params?: TypeListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = typeListSuspenseQueryKey(params);
  return queryOptions<
    TypeListQueryResponse,
    ResponseErrorConfig<Error>,
    TypeListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return typeList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Types are properties for Pokémon and their moves. Each type has three properties: which types of Pokémon it is super effective against, which types of Pokémon it is not very effective against, and which types of Pokémon it is completely ineffective against.
 * @summary List types
 * {@link /api/v2/type/}
 */
export function useTypeListSuspense<
  TData = TypeListQueryResponse,
  TQueryKey extends QueryKey = TypeListSuspenseQueryKey,
>(
  params?: TypeListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<TypeListQueryResponse, ResponseErrorConfig<Error>, TData, TQueryKey>
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? typeListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...typeListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
