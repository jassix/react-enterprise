/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GenerationRetrieveQueryResponse,
  GenerationRetrievePathParams,
} from "../models/GenerationRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { generationRetrieve } from "../clients/generationRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const generationRetrieveQueryKey = (id: GenerationRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/generation/:id/", params: { id: id } }] as const;

export type GenerationRetrieveQueryKey = ReturnType<typeof generationRetrieveQueryKey>;

export function generationRetrieveQueryOptions(
  id: GenerationRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = generationRetrieveQueryKey(id);
  return queryOptions<
    GenerationRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    GenerationRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return generationRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description A generation is a grouping of the Pokémon games that separates them based on the Pokémon they include. In each generation, a new set of Pokémon, Moves, Abilities and Types that did not exist in the previous generation are released.
 * @summary Get genration
 * {@link /api/v2/generation/:id/}
 */
export function useGenerationRetrieve<
  TData = GenerationRetrieveQueryResponse,
  TQueryData = GenerationRetrieveQueryResponse,
  TQueryKey extends QueryKey = GenerationRetrieveQueryKey,
>(
  id: GenerationRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GenerationRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? generationRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...generationRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
