/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonColorListQueryResponse,
  PokemonColorListQueryParams,
} from "../models/PokemonColorList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { pokemonColorList } from "../clients/pokemonColorList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const pokemonColorListQueryKey = (params?: PokemonColorListQueryParams) =>
  [{ url: "/api/v2/pokemon-color/" }, ...(params ? [params] : [])] as const;

export type PokemonColorListQueryKey = ReturnType<typeof pokemonColorListQueryKey>;

export function pokemonColorListQueryOptions(
  params?: PokemonColorListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonColorListQueryKey(params);
  return queryOptions<
    PokemonColorListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonColorListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonColorList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Colors used for sorting Pokémon in a Pokédex. The color listed in the Pokédex is usually the color most apparent or covering each Pokémon's body. No orange category exists; Pokémon that are primarily orange are listed as red or brown.
 * @summary List pokemon colors
 * {@link /api/v2/pokemon-color/}
 */
export function usePokemonColorList<
  TData = PokemonColorListQueryResponse,
  TQueryData = PokemonColorListQueryResponse,
  TQueryKey extends QueryKey = PokemonColorListQueryKey,
>(
  params?: PokemonColorListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PokemonColorListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonColorListQueryKey(params);

  const query = useQuery(
    {
      ...pokemonColorListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
