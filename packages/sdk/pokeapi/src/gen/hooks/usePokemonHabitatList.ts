/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonHabitatListQueryResponse,
  PokemonHabitatListQueryParams,
} from "../models/PokemonHabitatList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { pokemonHabitatList } from "../clients/pokemonHabitatList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const pokemonHabitatListQueryKey = (params?: PokemonHabitatListQueryParams) =>
  [{ url: "/api/v2/pokemon-habitat/" }, ...(params ? [params] : [])] as const;

export type PokemonHabitatListQueryKey = ReturnType<typeof pokemonHabitatListQueryKey>;

export function pokemonHabitatListQueryOptions(
  params?: PokemonHabitatListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonHabitatListQueryKey(params);
  return queryOptions<
    PokemonHabitatListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonHabitatListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonHabitatList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Habitats are generally different terrain Pokémon can be found in but can also be areas designated for rare or legendary Pokémon.
 * @summary List pokemom habitas
 * {@link /api/v2/pokemon-habitat/}
 */
export function usePokemonHabitatList<
  TData = PokemonHabitatListQueryResponse,
  TQueryData = PokemonHabitatListQueryResponse,
  TQueryKey extends QueryKey = PokemonHabitatListQueryKey,
>(
  params?: PokemonHabitatListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PokemonHabitatListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonHabitatListQueryKey(params);

  const query = useQuery(
    {
      ...pokemonHabitatListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
