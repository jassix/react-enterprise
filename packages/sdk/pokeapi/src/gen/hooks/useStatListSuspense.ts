/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { StatListQueryResponse, StatListQueryParams } from "../models/StatList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { statList } from "../clients/statList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const statListSuspenseQueryKey = (params?: StatListQueryParams) =>
  [{ url: "/api/v2/stat/" }, ...(params ? [params] : [])] as const;

export type StatListSuspenseQueryKey = ReturnType<typeof statListSuspenseQueryKey>;

export function statListSuspenseQueryOptions(
  params?: StatListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = statListSuspenseQueryKey(params);
  return queryOptions<
    StatListQueryResponse,
    ResponseErrorConfig<Error>,
    StatListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return statList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Stats determine certain aspects of battles. Each Pokémon has a value for each stat which grows as they gain levels and can be altered momentarily by effects in battles.
 * @summary List stats
 * {@link /api/v2/stat/}
 */
export function useStatListSuspense<
  TData = StatListQueryResponse,
  TQueryKey extends QueryKey = StatListSuspenseQueryKey,
>(
  params?: StatListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<StatListQueryResponse, ResponseErrorConfig<Error>, TData, TQueryKey>
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? statListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...statListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
