/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { ItemRetrieveQueryResponse, ItemRetrievePathParams } from "../models/ItemRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { itemRetrieve } from "../clients/itemRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const itemRetrieveQueryKey = (id: ItemRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/item/:id/", params: { id: id } }] as const;

export type ItemRetrieveQueryKey = ReturnType<typeof itemRetrieveQueryKey>;

export function itemRetrieveQueryOptions(
  id: ItemRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = itemRetrieveQueryKey(id);
  return queryOptions<
    ItemRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    ItemRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return itemRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description An item is an object in the games which the player can pick up, keep in their bag, and use in some manner. They have various uses, including healing, powering up, helping catch Pokémon, or to access a new area.
 * @summary Get item
 * {@link /api/v2/item/:id/}
 */
export function useItemRetrieve<
  TData = ItemRetrieveQueryResponse,
  TQueryData = ItemRetrieveQueryResponse,
  TQueryKey extends QueryKey = ItemRetrieveQueryKey,
>(
  id: ItemRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ItemRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? itemRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...itemRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
