/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { RegionListQueryResponse, RegionListQueryParams } from "../models/RegionList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { regionList } from "../clients/regionList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const regionListQueryKey = (params?: RegionListQueryParams) =>
  [{ url: "/api/v2/region/" }, ...(params ? [params] : [])] as const;

export type RegionListQueryKey = ReturnType<typeof regionListQueryKey>;

export function regionListQueryOptions(
  params?: RegionListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = regionListQueryKey(params);
  return queryOptions<
    RegionListQueryResponse,
    ResponseErrorConfig<Error>,
    RegionListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return regionList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description A region is an organized area of the Pokémon world. Most often, the main difference between regions is the species of Pokémon that can be encountered within them.
 * @summary List regions
 * {@link /api/v2/region/}
 */
export function useRegionList<
  TData = RegionListQueryResponse,
  TQueryData = RegionListQueryResponse,
  TQueryKey extends QueryKey = RegionListQueryKey,
>(
  params?: RegionListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        RegionListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? regionListQueryKey(params);

  const query = useQuery(
    {
      ...regionListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
