/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonShapeListQueryResponse,
  PokemonShapeListQueryParams,
} from "../models/PokemonShapeList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { pokemonShapeList } from "../clients/pokemonShapeList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const pokemonShapeListQueryKey = (params?: PokemonShapeListQueryParams) =>
  [{ url: "/api/v2/pokemon-shape/" }, ...(params ? [params] : [])] as const;

export type PokemonShapeListQueryKey = ReturnType<typeof pokemonShapeListQueryKey>;

export function pokemonShapeListQueryOptions(
  params?: PokemonShapeListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonShapeListQueryKey(params);
  return queryOptions<
    PokemonShapeListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonShapeListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonShapeList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Shapes used for sorting Pokémon in a Pokédex.
 * @summary List pokemon shapes
 * {@link /api/v2/pokemon-shape/}
 */
export function usePokemonShapeList<
  TData = PokemonShapeListQueryResponse,
  TQueryData = PokemonShapeListQueryResponse,
  TQueryKey extends QueryKey = PokemonShapeListQueryKey,
>(
  params?: PokemonShapeListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PokemonShapeListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonShapeListQueryKey(params);

  const query = useQuery(
    {
      ...pokemonShapeListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
