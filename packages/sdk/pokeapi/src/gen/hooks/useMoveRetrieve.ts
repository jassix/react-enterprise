/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MoveRetrieveQueryResponse, MoveRetrievePathParams } from "../models/MoveRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { moveRetrieve } from "../clients/moveRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const moveRetrieveQueryKey = (id: MoveRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/move/:id/", params: { id: id } }] as const;

export type MoveRetrieveQueryKey = ReturnType<typeof moveRetrieveQueryKey>;

export function moveRetrieveQueryOptions(
  id: MoveRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = moveRetrieveQueryKey(id);
  return queryOptions<
    MoveRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    MoveRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return moveRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Moves are the skills of Pokémon in battle. In battle, a Pokémon uses one move each turn. Some moves (including those learned by Hidden Machine) can be used outside of battle as well, usually for the purpose of removing obstacles or exploring new areas.
 * @summary Get move
 * {@link /api/v2/move/:id/}
 */
export function useMoveRetrieve<
  TData = MoveRetrieveQueryResponse,
  TQueryData = MoveRetrieveQueryResponse,
  TQueryKey extends QueryKey = MoveRetrieveQueryKey,
>(
  id: MoveRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MoveRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? moveRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...moveRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
