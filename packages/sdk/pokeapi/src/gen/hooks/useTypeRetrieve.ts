/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { TypeRetrieveQueryResponse, TypeRetrievePathParams } from "../models/TypeRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { typeRetrieve } from "../clients/typeRetrieve.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const typeRetrieveQueryKey = (id: TypeRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/type/:id/", params: { id: id } }] as const;

export type TypeRetrieveQueryKey = ReturnType<typeof typeRetrieveQueryKey>;

export function typeRetrieveQueryOptions(
  id: TypeRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = typeRetrieveQueryKey(id);
  return queryOptions<
    TypeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    TypeRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return typeRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Types are properties for Pokémon and their moves. Each type has three properties: which types of Pokémon it is super effective against, which types of Pokémon it is not very effective against, and which types of Pokémon it is completely ineffective against.
 * @summary Get types
 * {@link /api/v2/type/:id/}
 */
export function useTypeRetrieve<
  TData = TypeRetrieveQueryResponse,
  TQueryData = TypeRetrieveQueryResponse,
  TQueryKey extends QueryKey = TypeRetrieveQueryKey,
>(
  id: TypeRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        TypeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? typeRetrieveQueryKey(id);

  const query = useQuery(
    {
      ...typeRetrieveQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
