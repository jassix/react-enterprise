/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { TypeListQueryResponse, TypeListQueryParams } from "../models/TypeList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { typeList } from "../clients/typeList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const typeListQueryKey = (params?: TypeListQueryParams) =>
  [{ url: "/api/v2/type/" }, ...(params ? [params] : [])] as const;

export type TypeListQueryKey = ReturnType<typeof typeListQueryKey>;

export function typeListQueryOptions(
  params?: TypeListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = typeListQueryKey(params);
  return queryOptions<
    TypeListQueryResponse,
    ResponseErrorConfig<Error>,
    TypeListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return typeList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Types are properties for Pokémon and their moves. Each type has three properties: which types of Pokémon it is super effective against, which types of Pokémon it is not very effective against, and which types of Pokémon it is completely ineffective against.
 * @summary List types
 * {@link /api/v2/type/}
 */
export function useTypeList<
  TData = TypeListQueryResponse,
  TQueryData = TypeListQueryResponse,
  TQueryKey extends QueryKey = TypeListQueryKey,
>(
  params?: TypeListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        TypeListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? typeListQueryKey(params);

  const query = useQuery(
    {
      ...typeListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
