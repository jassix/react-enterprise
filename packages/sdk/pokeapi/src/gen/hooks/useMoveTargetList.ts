/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  MoveTargetListQueryResponse,
  MoveTargetListQueryParams,
} from "../models/MoveTargetList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { moveTargetList } from "../clients/moveTargetList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const moveTargetListQueryKey = (params?: MoveTargetListQueryParams) =>
  [{ url: "/api/v2/move-target/" }, ...(params ? [params] : [])] as const;

export type MoveTargetListQueryKey = ReturnType<typeof moveTargetListQueryKey>;

export function moveTargetListQueryOptions(
  params?: MoveTargetListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = moveTargetListQueryKey(params);
  return queryOptions<
    MoveTargetListQueryResponse,
    ResponseErrorConfig<Error>,
    MoveTargetListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return moveTargetList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Targets moves can be directed at during battle. Targets can be Pokémon, environments or even other moves.
 * @summary List move targets
 * {@link /api/v2/move-target/}
 */
export function useMoveTargetList<
  TData = MoveTargetListQueryResponse,
  TQueryData = MoveTargetListQueryResponse,
  TQueryKey extends QueryKey = MoveTargetListQueryKey,
>(
  params?: MoveTargetListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        MoveTargetListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? moveTargetListQueryKey(params);

  const query = useQuery(
    {
      ...moveTargetListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
