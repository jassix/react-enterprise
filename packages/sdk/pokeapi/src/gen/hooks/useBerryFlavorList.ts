/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  BerryFlavorListQueryResponse,
  BerryFlavorListQueryParams,
} from "../models/BerryFlavorList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { berryFlavorList } from "../clients/berryFlavorList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const berryFlavorListQueryKey = (params?: BerryFlavorListQueryParams) =>
  [{ url: "/api/v2/berry-flavor/" }, ...(params ? [params] : [])] as const;

export type BerryFlavorListQueryKey = ReturnType<typeof berryFlavorListQueryKey>;

export function berryFlavorListQueryOptions(
  params?: BerryFlavorListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = berryFlavorListQueryKey(params);
  return queryOptions<
    BerryFlavorListQueryResponse,
    ResponseErrorConfig<Error>,
    BerryFlavorListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return berryFlavorList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Flavors determine whether a Pokémon will benefit or suffer from eating a berry based on their **nature**. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Flavor) for greater detail.
 * @summary List berry flavors
 * {@link /api/v2/berry-flavor/}
 */
export function useBerryFlavorList<
  TData = BerryFlavorListQueryResponse,
  TQueryData = BerryFlavorListQueryResponse,
  TQueryKey extends QueryKey = BerryFlavorListQueryKey,
>(
  params?: BerryFlavorListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        BerryFlavorListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? berryFlavorListQueryKey(params);

  const query = useQuery(
    {
      ...berryFlavorListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
