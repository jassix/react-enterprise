/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GenderListQueryResponse, GenderListQueryParams } from "../models/GenderList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { genderList } from "../clients/genderList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const genderListQueryKey = (params?: GenderListQueryParams) =>
  [{ url: "/api/v2/gender/" }, ...(params ? [params] : [])] as const;

export type GenderListQueryKey = ReturnType<typeof genderListQueryKey>;

export function genderListQueryOptions(
  params?: GenderListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = genderListQueryKey(params);
  return queryOptions<
    GenderListQueryResponse,
    ResponseErrorConfig<Error>,
    GenderListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return genderList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Genders were introduced in Generation II for the purposes of breeding Pokémon but can also result in visual differences or even different evolutionary lines. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Gender) for greater detail.
 * @summary List genders
 * {@link /api/v2/gender/}
 */
export function useGenderList<
  TData = GenderListQueryResponse,
  TQueryData = GenderListQueryResponse,
  TQueryKey extends QueryKey = GenderListQueryKey,
>(
  params?: GenderListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GenderListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? genderListQueryKey(params);

  const query = useQuery(
    {
      ...genderListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
