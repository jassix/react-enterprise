/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  PokemonSpeciesListQueryResponse,
  PokemonSpeciesListQueryParams,
} from "../models/PokemonSpeciesList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { pokemonSpeciesList } from "../clients/pokemonSpeciesList.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const pokemonSpeciesListQueryKey = (params?: PokemonSpeciesListQueryParams) =>
  [{ url: "/api/v2/pokemon-species/" }, ...(params ? [params] : [])] as const;

export type PokemonSpeciesListQueryKey = ReturnType<typeof pokemonSpeciesListQueryKey>;

export function pokemonSpeciesListQueryOptions(
  params?: PokemonSpeciesListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = pokemonSpeciesListQueryKey(params);
  return queryOptions<
    PokemonSpeciesListQueryResponse,
    ResponseErrorConfig<Error>,
    PokemonSpeciesListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return pokemonSpeciesList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description A Pokémon Species forms the basis for at least one Pokémon. Attributes of a Pokémon species are shared across all varieties of Pokémon within the species. A good example is Wormadam; Wormadam is the species which can be found in three different varieties, Wormadam-Trash, Wormadam-Sandy and Wormadam-Plant.
 * @summary List pokemon species
 * {@link /api/v2/pokemon-species/}
 */
export function usePokemonSpeciesList<
  TData = PokemonSpeciesListQueryResponse,
  TQueryData = PokemonSpeciesListQueryResponse,
  TQueryKey extends QueryKey = PokemonSpeciesListQueryKey,
>(
  params?: PokemonSpeciesListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        PokemonSpeciesListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? pokemonSpeciesListQueryKey(params);

  const query = useQuery(
    {
      ...pokemonSpeciesListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
