/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GenerationListQueryResponse,
  GenerationListQueryParams,
} from "../models/GenerationList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { generationList } from "../clients/generationList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const generationListSuspenseQueryKey = (params?: GenerationListQueryParams) =>
  [{ url: "/api/v2/generation/" }, ...(params ? [params] : [])] as const;

export type GenerationListSuspenseQueryKey = ReturnType<typeof generationListSuspenseQueryKey>;

export function generationListSuspenseQueryOptions(
  params?: GenerationListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = generationListSuspenseQueryKey(params);
  return queryOptions<
    GenerationListQueryResponse,
    ResponseErrorConfig<Error>,
    GenerationListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return generationList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description A generation is a grouping of the Pokémon games that separates them based on the Pokémon they include. In each generation, a new set of Pokémon, Moves, Abilities and Types that did not exist in the previous generation are released.
 * @summary List genrations
 * {@link /api/v2/generation/}
 */
export function useGenerationListSuspense<
  TData = GenerationListQueryResponse,
  TQueryKey extends QueryKey = GenerationListSuspenseQueryKey,
>(
  params?: GenerationListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GenerationListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? generationListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...generationListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
