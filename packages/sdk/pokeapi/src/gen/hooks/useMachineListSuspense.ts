/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { MachineListQueryResponse, MachineListQueryParams } from "../models/MachineList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { machineList } from "../clients/machineList.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const machineListSuspenseQueryKey = (params?: MachineListQueryParams) =>
  [{ url: "/api/v2/machine/" }, ...(params ? [params] : [])] as const;

export type MachineListSuspenseQueryKey = ReturnType<typeof machineListSuspenseQueryKey>;

export function machineListSuspenseQueryOptions(
  params?: MachineListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = machineListSuspenseQueryKey(params);
  return queryOptions<
    MachineListQueryResponse,
    ResponseErrorConfig<Error>,
    MachineListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return machineList(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Machines are the representation of items that teach moves to Pokémon. They vary from version to version, so it is not certain that one specific TM or HM corresponds to a single Machine.
 * @summary List machines
 * {@link /api/v2/machine/}
 */
export function useMachineListSuspense<
  TData = MachineListQueryResponse,
  TQueryKey extends QueryKey = MachineListSuspenseQueryKey,
>(
  params?: MachineListQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        MachineListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? machineListSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...machineListSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
