/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GenderRetrieveQueryResponse,
  GenderRetrievePathParams,
} from "../models/GenderRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { genderRetrieve } from "../clients/genderRetrieve.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const genderRetrieveSuspenseQueryKey = (id: GenderRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/gender/:id/", params: { id: id } }] as const;

export type GenderRetrieveSuspenseQueryKey = ReturnType<typeof genderRetrieveSuspenseQueryKey>;

export function genderRetrieveSuspenseQueryOptions(
  id: GenderRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = genderRetrieveSuspenseQueryKey(id);
  return queryOptions<
    GenderRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    GenderRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return genderRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Genders were introduced in Generation II for the purposes of breeding Pokémon but can also result in visual differences or even different evolutionary lines. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Gender) for greater detail.
 * @summary Get gender
 * {@link /api/v2/gender/:id/}
 */
export function useGenderRetrieveSuspense<
  TData = GenderRetrieveQueryResponse,
  TQueryKey extends QueryKey = GenderRetrieveSuspenseQueryKey,
>(
  id: GenderRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GenderRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? genderRetrieveSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...genderRetrieveSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
