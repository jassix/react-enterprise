/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { TypeRetrieveQueryResponse, TypeRetrievePathParams } from "../models/TypeRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { typeRetrieve } from "../clients/typeRetrieve.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const typeRetrieveSuspenseQueryKey = (id: TypeRetrievePathParams["id"] | undefined) =>
  [{ url: "/api/v2/type/:id/", params: { id: id } }] as const;

export type TypeRetrieveSuspenseQueryKey = ReturnType<typeof typeRetrieveSuspenseQueryKey>;

export function typeRetrieveSuspenseQueryOptions(
  id: TypeRetrievePathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = typeRetrieveSuspenseQueryKey(id);
  return queryOptions<
    TypeRetrieveQueryResponse,
    ResponseErrorConfig<Error>,
    TypeRetrieveQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return typeRetrieve(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Types are properties for Pokémon and their moves. Each type has three properties: which types of Pokémon it is super effective against, which types of Pokémon it is not very effective against, and which types of Pokémon it is completely ineffective against.
 * @summary Get types
 * {@link /api/v2/type/:id/}
 */
export function useTypeRetrieveSuspense<
  TData = TypeRetrieveQueryResponse,
  TQueryKey extends QueryKey = TypeRetrieveSuspenseQueryKey,
>(
  id: TypeRetrievePathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        TypeRetrieveQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? typeRetrieveSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...typeRetrieveSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
