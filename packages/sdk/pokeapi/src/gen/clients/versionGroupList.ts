/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  VersionGroupListQueryResponse,
  VersionGroupListQueryParams,
} from "../models/VersionGroupList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getVersionGroupListUrl() {
  const res = { method: "GET", url: `/api/v2/version-group/` as const };
  return res;
}

/**
 * @description Version groups categorize highly similar versions of the games.
 * @summary List version groups
 * {@link /api/v2/version-group/}
 */
export async function versionGroupList(
  params?: VersionGroupListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<VersionGroupListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getVersionGroupListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
