/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  ItemPocketListQueryResponse,
  ItemPocketListQueryParams,
} from "../models/ItemPocketList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getItemPocketListUrl() {
  const res = { method: "GET", url: `/api/v2/item-pocket/` as const };
  return res;
}

/**
 * @description Pockets within the players bag used for storing items by category.
 * @summary List item pockets
 * {@link /api/v2/item-pocket/}
 */
export async function itemPocketList(
  params?: ItemPocketListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<ItemPocketListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getItemPocketListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
