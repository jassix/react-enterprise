/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { LocationListQueryResponse, LocationListQueryParams } from "../models/LocationList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getLocationListUrl() {
  const res = { method: "GET", url: `/api/v2/location/` as const };
  return res;
}

/**
 * @description Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
 * @summary List locations
 * {@link /api/v2/location/}
 */
export async function locationList(
  params?: LocationListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<LocationListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getLocationListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
