/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  MoveCategoryRetrieveQueryResponse,
  MoveCategoryRetrievePathParams,
} from "../models/MoveCategoryRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getMoveCategoryRetrieveUrl(id: MoveCategoryRetrievePathParams["id"]) {
  const res = { method: "GET", url: `/api/v2/move-category/${id}/` as const };
  return res;
}

/**
 * @description Very general categories that loosely group move effects.
 * @summary Get move meta category
 * {@link /api/v2/move-category/:id/}
 */
export async function moveCategoryRetrieve(
  id: MoveCategoryRetrievePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<MoveCategoryRetrieveQueryResponse, ResponseErrorConfig<Error>, unknown>(
    { method: "GET", url: getMoveCategoryRetrieveUrl(id).url.toString(), ...requestConfig },
  );
  return res.data;
}
