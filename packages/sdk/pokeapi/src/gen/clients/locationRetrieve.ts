/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  LocationRetrieveQueryResponse,
  LocationRetrievePathParams,
} from "../models/LocationRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getLocationRetrieveUrl(id: LocationRetrievePathParams["id"]) {
  const res = { method: "GET", url: `/api/v2/location/${id}/` as const };
  return res;
}

/**
 * @description Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
 * @summary Get location
 * {@link /api/v2/location/:id/}
 */
export async function locationRetrieve(
  id: LocationRetrievePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<LocationRetrieveQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getLocationRetrieveUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
