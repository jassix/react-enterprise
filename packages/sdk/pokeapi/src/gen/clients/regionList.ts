/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { RegionListQueryResponse, RegionListQueryParams } from "../models/RegionList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getRegionListUrl() {
  const res = { method: "GET", url: `/api/v2/region/` as const };
  return res;
}

/**
 * @description A region is an organized area of the Pokémon world. Most often, the main difference between regions is the species of Pokémon that can be encountered within them.
 * @summary List regions
 * {@link /api/v2/region/}
 */
export async function regionList(
  params?: RegionListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<RegionListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getRegionListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
