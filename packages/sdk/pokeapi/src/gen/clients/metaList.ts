/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { MetaListQueryResponse } from "../models/MetaList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getMetaListUrl() {
  const res = { method: "GET", url: `/api/v2/meta/` as const };
  return res;
}

/**
 * @description Returns metadata about the current deployed version of the API, including the git commit hash, deploy date, and tag (if any).
 * {@link /api/v2/meta/}
 */
export async function metaList(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<MetaListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getMetaListUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
