/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  PokemonShapeListQueryResponse,
  PokemonShapeListQueryParams,
} from "../models/PokemonShapeList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getPokemonShapeListUrl() {
  const res = { method: "GET", url: `/api/v2/pokemon-shape/` as const };
  return res;
}

/**
 * @description Shapes used for sorting Pokémon in a Pokédex.
 * @summary List pokemon shapes
 * {@link /api/v2/pokemon-shape/}
 */
export async function pokemonShapeList(
  params?: PokemonShapeListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<PokemonShapeListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getPokemonShapeListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
