/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { StatRetrieveQueryResponse, StatRetrievePathParams } from "../models/StatRetrieve.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getStatRetrieveUrl(id: StatRetrievePathParams["id"]) {
  const res = { method: "GET", url: `/api/v2/stat/${id}/` as const };
  return res;
}

/**
 * @description Stats determine certain aspects of battles. Each Pokémon has a value for each stat which grows as they gain levels and can be altered momentarily by effects in battles.
 * @summary Get stat
 * {@link /api/v2/stat/:id/}
 */
export async function statRetrieve(
  id: StatRetrievePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<StatRetrieveQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getStatRetrieveUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
