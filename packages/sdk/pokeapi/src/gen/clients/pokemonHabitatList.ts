/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  PokemonHabitatListQueryResponse,
  PokemonHabitatListQueryParams,
} from "../models/PokemonHabitatList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getPokemonHabitatListUrl() {
  const res = { method: "GET", url: `/api/v2/pokemon-habitat/` as const };
  return res;
}

/**
 * @description Habitats are generally different terrain Pokémon can be found in but can also be areas designated for rare or legendary Pokémon.
 * @summary List pokemom habitas
 * {@link /api/v2/pokemon-habitat/}
 */
export async function pokemonHabitatList(
  params?: PokemonHabitatListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<PokemonHabitatListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getPokemonHabitatListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
