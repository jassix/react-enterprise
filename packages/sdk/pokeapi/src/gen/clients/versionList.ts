/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { VersionListQueryResponse, VersionListQueryParams } from "../models/VersionList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getVersionListUrl() {
  const res = { method: "GET", url: `/api/v2/version/` as const };
  return res;
}

/**
 * @description Versions of the games, e.g., Red, Blue or Yellow.
 * @summary List versions
 * {@link /api/v2/version/}
 */
export async function versionList(
  params?: VersionListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<VersionListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getVersionListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
