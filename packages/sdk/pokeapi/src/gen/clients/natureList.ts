/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { NatureListQueryResponse, NatureListQueryParams } from "../models/NatureList.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getNatureListUrl() {
  const res = { method: "GET", url: `/api/v2/nature/` as const };
  return res;
}

/**
 * @description Natures influence how a Pokémon's stats grow. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Nature) for greater detail.
 * @summary List natures
 * {@link /api/v2/nature/}
 */
export async function natureList(
  params?: NatureListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<NatureListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: "GET",
    url: getNatureListUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
